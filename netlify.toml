[build.environment]
    NPM_FLAGS = "--prefix=/dev/null"
    NODE_VERSION = "16.3.0"
    
[build]
  command = "pnpm run build --force || ( npm install pnpm && pnpm run build --force )"
  publish = "build/"
  functions = "functions/"